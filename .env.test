# Used only so pydantic-settings has values to load during local test
# collection when no real .env is present. Real runs use docker-compose's .env.
DATABASE_URL=postgresql+psycopg2://ledger_admin:change_me_in_prod@localhost:5432/ledger_db
ASYNC_DATABASE_URL=postgresql+asyncpg://ledger_admin:change_me_in_prod@localhost:5432/ledger_db
JWT_SECRET_KEY=test-secret-not-for-production
